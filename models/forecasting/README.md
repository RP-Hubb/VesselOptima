# VesselOptima — Forecasting Models Directory

This directory serves as the extended local workspace for serialized walk-forward forecasting models.
Primary model registry artifacts reside under `models/forecast/<target>/<series_id>/<version>/`.
